import React from 'react'
import themeContext from '../themeContext'

function Main (props) {
    return (
      <div className="main">
      <button>Display Theme</button>
      </div>
    )
}

export default Main
